#Ruleset for Fail2Ban to block IP addresses that have touched the honeypot

### UNTESTED WONT WORK ###

#/etc/failban/jail.local
[fail2ban-honeypot]
enabled = true
filter = honeypot.conf
action = iptables-multiport[name=Honeypot, port="http,https", protocol=tcp]
logpath = /var/log/apache2/error.log
maxretry = 1
findtime = 600
bantime = 86400

#/etc/failban/filter.d/honeypot.conf
[Definition]
failregex = Fail2Ban: <HOST> tried to access
